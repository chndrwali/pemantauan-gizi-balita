const Page = async () => {
  return (
    <div className="min-w-screen min-h-screen flex items-center justify-center">
      {/* {JSON.stringify(data)}
      {data && 
      } */}
    </div>
  );
};

export default Page;
