import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Admin',
};

// export const dynamic = 'force-dynamic';

const Page = () => {
  return <div></div>;
};

export default Page;
